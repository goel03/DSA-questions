bool checkArmstrong(int n){
	int sum=0;
	int x=n;
	int last=0;

	while(n>0)   //if n 167
	{
     last=n%10;
	 
	 sum=sum+(last*last*last);
	 n=n/10;
	}
	if(sum==x)
	{
		return true;
	}
	return false;
}
