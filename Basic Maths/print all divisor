
#include <bits/stdc++.h>
vector<int> printDivisors(int n) {
    vector<int>v;

     
//one way

for(int i=1;i<=sqrt(n);i++)   
{
    if(n%i==0)
    {
        v.push_back(i);
        if((n/i)!=i)
        {
            v.push_back(n/i);   
        }
    }

    sort(v.begin(),v.end());
   
}

    // for(int i=1;i<=n;i++)
    // {
    //     if(n%i==0)
    //     {
    //         v.push_back(i);
    //     }

    // }
    return v;
}
